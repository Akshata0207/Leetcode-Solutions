class Solution:
    def gcdOfStrings(self, str1: str, str2: str) -> str:
        ans = ""
        if ((str1+str2) != (str2+str1)):
            return ""
        n = math.gcd(len(str1),len(str2))
        for i in range(n):
            ans += str1[i]
        return ans
