class Solution:
    def removeElement(self, nums: List[int], val: int) -> int:
        i = 0
        j = len(nums)
        while (i<j):
            if nums[i] == val:
                nums.pop(i)
                j = j-1
            else:
                i = i+1
        return j
